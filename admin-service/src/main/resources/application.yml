server:
  port: 8085

spring:
  application:
    name: admin-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        fail-fast: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
